# Browser Extension

This app will contain the Manifest V3 extension in Milestone 2.

Milestone 1 intentionally does not implement the popup, content scripts, or DOM extraction. The current milestone focuses on the shared package and local Bridge core so the file-writing pipeline is correct before the browser UI is added.
